<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufop.nathany.futmannathany">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/camisa"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ConfirmarJogadores"
            android:label="@string/title_activity_listar_jogadores"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ConfigurarPelada" />
        <activity android:name=".ListarEstatisticas" />
        <activity android:name=".CadastrarJogadores" />
        <activity
            android:name=".ListarJogadores"
            android:label="@string/title_activity_listar_jogadores"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EditarJogador" />
        <activity android:name=".Pelada" />
        <activity android:name=".PartidaAtual" />
        <activity android:name=".EmEspera"></activity>
    </application>

</manifest>